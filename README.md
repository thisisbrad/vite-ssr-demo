# ssr-signals

To install dependencies:

```bash
bun install
```

To run:

```bash
bun run dev
```

To build to JS:

```bash
bun run build
```
To preview with Vite:

```bash
bun run build
```

```
my-ssr-test/
├── index.html
├── package.json
├── vite.config.js
├── server.js
└── src/
    ├── App.js
    ├── entry-client.js
    └── entry-server.js
```
